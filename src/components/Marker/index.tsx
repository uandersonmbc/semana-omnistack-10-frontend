import React from 'react';

import { Container } from './styles';

import { MarkerProps } from './../../shared/types';

const Marker: React.FC<MarkerProps> = (props) => {
    return (
        <Container />
    );
}

export default Marker;
